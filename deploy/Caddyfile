# Replace api.example.com with your real API hostname (e.g. api.yourdomain.com).
# Point DNS A/AAAA for that hostname to this VPS IP before starting Caddy.

apiv2.ticsummit.org {
	encode gzip zstd
	reverse_proxy api:5005
}
